```
./build.sh # (Optionally) build image locally (instead of pulling from ghcr.io)

./run.sh # Create and enter new container

build # Alias to compile (modified) source code inside container

run # Alias to run source code
```
